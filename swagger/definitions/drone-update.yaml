type: object
properties:
  baseLongitude:
    $ref: '#/definitions/Longitude'
  baseLatitude:
    $ref: '#/definitions/Latitude'
  price:
    $ref: '#/definitions/Decimal'
  batteryPower:
    type: number
    minimum: 0
  enginePower:
    type: number
    minimum: 0
  loadCapacity:
    type: number
    minimum: 0
  canCarryLiquids:
    type: boolean
additionalProperties: false