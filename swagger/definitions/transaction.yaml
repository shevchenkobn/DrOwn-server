type: object
required:
- transactionId
- createdAt
- priceId
- userId
- period
- status
properties:
  transactionId:
    $ref: '#/definitions/BigInt'
  createdAt:
    $ref: '#/definitions/Date'
  priceId:
    $ref: '#/definitions/BigInt'
  userId:
    $ref: '#/definitions/BigInt'
  period:
    type: integer
    minimum: 1
  status:
    type: integer
    minimum: 0
    maximum: 2
additionalProperties: false