x-swagger-router-controller: user-helpers

get:
  tags:
  - users
  - no-auth
  operationId: getUserStatuses
  summary: Returns all user statuses
  responses:
    200:
      description: OK
      schema:
        $ref: '#/definitions/UserStatuses'
