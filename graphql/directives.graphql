enum StringType {
    PHONE
    EMAIL
}

directive @string(
    maxLength: Int
    type: StringType
    regex: String
) on INPUT_FIELD_DEFINITION

directive @authorized(
    role: UserRoles
) on QUERY
| MUTATION
| SUBSCRIPTION
| FIELD
| FRAGMENT_DEFINITION
| FRAGMENT_SPREAD
| INLINE_FRAGMENT
| SCHEMA
| SCALAR
| OBJECT
| FIELD_DEFINITION
| ARGUMENT_DEFINITION
| INTERFACE
| UNION
| ENUM
| ENUM_VALUE
