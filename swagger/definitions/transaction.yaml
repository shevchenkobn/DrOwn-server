type: object
required:
- transactionId
- createdAt
- droneId
- userId
- period
properties:
  transactionId:
    $ref: '#/definitions/BigInt'
  createdAt:
    $ref: '#/definitions/Date'
  droneId:
    $ref: '#/definitions/BigInt'
  userId:
    $ref: '#/definitions/BigInt'
  period:
    type: integer
    minimum: 1
additionalProperties: false