type: object
allOf:
- $ref: '#/definitions/UserBase'
required:
- email
- role
- name
- password
properties:
  password:
    type: string
    format: password
    pattern: ^(?=.*[A-Z])(?=.*[!@#$&*])(?=.*[0-9])(?=.*[a-z]).{6,}$