x-swagger-router-controller: user-helpers

get:
  tags:
  - users
  operationId: getUserStatuses
  summary: Returns all user roles
  responses:
    200:
      description: OK
      schema:
        $ref: '#/definitions/UserRoles'
