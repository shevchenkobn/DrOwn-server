type: object
properties:
  ownerId:
    $ref: '#/definitions/BigInt'
  baseLongitude:
    $ref: '#/definitions/Longitude'
  baseLatitude:
    $ref: '#/definitions/Latitude'
  batteryPower:
    type: number
    minimum: 0
  enginePower:
    type: number
    minimum: 0
  loadCapacity:
    type: number
    minimum: 0
  canCarryLiquids:
    type: boolean
  isWritingTelemetry:
    type: boolean
additionalProperties: false